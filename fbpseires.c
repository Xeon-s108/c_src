/* This program prints the sum of [n!/npow(n)] series! */
#include<stdio.h>
#include<math.h>
void main ()
{
float i , num, den, limit, temp;
printf("Enter the number of last term in series : ");
scanf("%f",&limit);
for (i=1;i<=limit;i++)
    {
        
    }
}