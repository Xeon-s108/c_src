#include<stdio.h>
int linear_srch (char find)
{
for (i=0;i)
}

void main()
{
    char  str = 'aman, jyoti, subhasha, malviya, kartikey', srch;
    int index;
    printf("Enter the name to search : ");
    gets(srch);
    index = linear_srch(srch);

}